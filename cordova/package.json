{
  "name": "io.cordova.nextapp",
  "displayName": "NextApp",
  "version": "1.0.0",
  "description": "A sample Apache Cordova application that responds to the deviceready event.",
  "main": "index.js",
  "scripts": {
    "cordova:setup": "npm install && cordova platform add android",
    "cordova:check": "cordova requirements && cordova platform ls",
    "android:prebuild": "cordova platform rm android && cordova platform add android",
    "android:emulate": "cordova build android && cordova run android"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Apache Cordova Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "cordova-android": "^10.1.2",
    "cordova-ios": "^6.2.0"
  },
  "cordova": {
    "platforms": [
      "android",
      "ios"
    ]
  }
}